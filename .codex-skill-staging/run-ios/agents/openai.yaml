interface:
  display_name: "Run iOS"
  short_description: "Run the current iOS app on iPhone."
  default_prompt: "Use $run-ios to build, install, and launch the current iOS project on a connected physical iPhone from the real project directory."

policy:
  allow_implicit_invocation: true
