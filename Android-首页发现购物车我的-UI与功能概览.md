# Android 四大主 Tab 界面 UI 概览与功能逻辑

## 一、宿主页结构总览

Android 端四个主 Tab 挂载在 `MainActivity` 内，通过底部导航 + `ViewPager` 进行切换。

当前底部导航文案为：

1. 首页
2. 发现
3. 购物车
4. 我的

补充说明：

1. 宿主页源码实际还预留了“消息”入口，但是否展示取决于 `Constans.SHOW_MESSAGE_ENTRY`。
2. 本文只聚焦你指定的四个主 Tab：`首页 / 发现 / 购物车 / 我的`。
3. `购物车` 和 `我的` 在底部点击时带登录拦截，未登录会先跳登录页，再回到对应页。

主入口相关文件：

- `MainActivity`
- `MainIndexFragment`
- `base_bottom_menu_layout.xml`

## 二、四个 Tab 总览

| Tab | Android 页面承载 | 页面定位 | 当前特征 |
| --- | --- | --- | --- |
| 首页 | `MainIndexFragment` | 首页总入口与内容分发中心 | 顶部城市/搜索/查档，二级分栏为婚庆/社团/案例 |
| 发现 | `FindFragment` | 圈层内容浏览与互动页 | 当前主标题是“婚庆圈子”，发布按钮悬浮，内容页为发现流 |
| 购物车 | `NewCartFragment` | 待结算与购物车管理页 | 当前顶部只显示“购物车”，底层保留婚庆/商城双购物车能力 |
| 我的 | `MineFragment` | 用户中心与业务入口聚合页 | 个人信息、订单、接单、店铺管理、工具、VIP/充值等集中分发 |

## 三、首页

### 1. 页面定位

Android 首页不是单纯的一个列表，而是一个“首页容器 + 二级频道”的结构。

外层由 `MainIndexFragment` 承载，顶部统一包含：

1. 城市切换
2. 搜索框
3. 查档入口
4. 二级频道指示器

首页内部分为三个二级频道：

1. 婚庆
2. 社团
3. 案例

对应承载 Fragment：

1. `NewIndexFragment`
2. `ClubFragment`
3. `NewExampleFragment`

### 2. UI 结构概览

#### 2.1 首页外层

1. 顶部状态栏占位
2. 城市选择
3. 搜索输入框入口
4. “查档”快捷入口
5. `MagicIndicator` 分栏导航
6. 下方 `ViewPager` 内容区

#### 2.2 婚庆频道 `NewIndexFragment`

根据 `RecyclerView + 多种 Holder` 组合页面，核心模块包括：

1. Banner 轮播
2. 婚庆分类宫格，支持分页滑动
3. 小广告位/活动位
4. “婚礼案例”“免费获取方案”等营销入口
5. 常用工具区
6. 热门活动区
7. 热门团队商家
8. 热门个人商家
9. 猜你喜欢内容流

“猜你喜欢”内容类型并不单一，实际可能包含：

1. 案例
2. 图册
3. 视频
4. 报价/店铺内容

#### 2.3 社团频道 `ClubFragment`

1. 顶部筛选条
2. 筛选项为：
   - 全部
   - 综合排序
   - 全区域
   - 筛选
3. 下方社团列表

列表项展示信息主要包括：

1. 社团封面
2. 社团名称
3. 起步价格
4. 成员数量
5. 类型/职位
6. 地址

#### 2.4 案例频道

首页第三栏为案例频道，由 `NewExampleFragment` 承载，承担案例浏览和详情跳转职能。

### 3. 功能逻辑

1. 首页默认进入“婚庆”频道。
2. 城市切换通过 `CityListActivity` 触发，首页会保存城市并联动刷新。
3. 搜索点击进入 `SearchActivity`。
4. “查档”点击进入 `HistoryActivity`。
5. 婚庆频道通过接口组合拉取首页数据和婚庆分类数据。
6. 分类宫格点击后跳转分类列表或全部分类页。
7. Banner、广告位、活动位都带跳转逻辑，不是纯展示。
8. “猜你喜欢”内容会根据类型跳转到案例详情、图册预览、视频播放、报价详情或商家详情。
9. 商家卡片支持关注/取消关注。
10. 社团频道支持刷新、分页、筛选组合查询，并跳转 `NewClubDetailsActivity`。

### 4. Android 端特征总结

相比小程序，Android 首页更明确地把“社团”和“案例”放进首页二级频道中，而不是把四个 Tab 纯平铺成四个独立业务页。

因此 Android 的“首页”实际上承担了三层职责：

1. 首页婚庆流量入口
2. 社团浏览入口
3. 案例浏览入口

## 四、发现

### 1. 页面定位

Android 发现页是内容圈子浏览页，当前实现重点放在婚庆圈内容流、筛选、关注与发布。

承载页面：`FindFragment`

子列表页面：`fragment.discover.DiscoverFragment`

### 2. UI 结构概览

#### 2.1 外层结构

1. 顶部状态栏占位
2. 页面标题，当前直接显示“婚庆圈子”
3. 顶部分栏 `TabLayout` 预留但默认隐藏
4. 右下角悬浮发布按钮
5. 下方内容 `ViewPager`

#### 2.2 子内容页结构

发现子页当前主要包含：

1. 职业筛选入口
2. 排序切换：
   - 最新
   - 最热
   - 关注
3. 列表内容区
4. 空态文案

#### 2.3 内容卡片结构

列表项展示内容包括：

1. 用户头像
2. 用户昵称
3. 职业
4. 发布时间
5. 团队名称
6. 文本内容
7. 九宫格图片
8. 浏览数
9. 评论数
10. 点赞数
11. 关注按钮

### 3. 功能逻辑

1. 页面默认进入发现列表第一页。
2. 发现子页支持下拉刷新与上拉分页。
3. “最新 / 最热 / 关注”切换后会重新拉取列表。
4. 婚庆圈支持职业分类筛选，通过弹窗选择职业后刷新列表。
5. 点击卡片进入 `DiscoverDetailActivity`。
6. 点击头像可进入商家/店铺详情。
7. 支持关注、取消关注。
8. 支持点赞、取消点赞。
9. 支持图片预览。
10. 发布按钮点击后，如果未登录先跳登录；已登录进入 `AddActivitiesActivity`。

### 4. Android 端特征总结

Android 发现页与小程序版最大的区别是：

1. 当前外层标题更偏固定页头，直接显示“婚庆圈子”。
2. 虽然代码里保留了商城圈能力，但顶部 `TabLayout` 目前隐藏，不是一个明显的双圈切换界面。
3. 当前实现更像“单主场景内容流 + 可选扩展”，而不是小程序那种强双圈并列。

## 五、购物车

### 1. 页面定位

Android 购物车页承接交易前的选择、改数量、删除、合计与结算操作。

承载页面：`NewCartFragment`

默认购物车主体：`WeddingCartFragment`

### 2. UI 结构概览

#### 2.1 外层结构

1. 顶部状态栏占位
2. 页头标题“购物车”或“购物车(n)”
3. 顶部分栏 `TabLayout` 预留但当前隐藏
4. 下方购物车内容区

#### 2.2 当前主内容结构

`WeddingCartFragment` 的核心区域包括：

1. 购物车列表
2. 店铺分组区块
3. 商品条目
4. 全选勾选
5. 实时合计金额
6. 去结算按钮
7. 空购物车态
8. 猜你喜欢推荐区

#### 2.3 商品项内容

1. 店铺级勾选
2. 商品级勾选
3. 商品图
4. 商品名称
5. 规格/描述
6. 单价
7. 数量修改
8. 左滑删除

### 3. 功能逻辑

1. 在 `MainActivity` 点击购物车时，未登录会先跳登录，不直接切页。
2. 登录后进入购物车页，顶部会请求购物车数量并回显到标题。
3. 购物车支持按店铺分组。
4. 支持商品勾选、店铺勾选、全选。
5. 选中状态会实时联动总价计算。
6. 支持修改购买数量。
7. 支持删除购物车商品。
8. 选中商品后可进入 `NewSureOrderActivity` 结算。
9. 页面底层保留“婚庆购物车 / 商城购物车”双能力，但当前顶部切换 Tab 被隐藏，现有 UI 更偏单页购物车。
10. 推荐区承担空态补充曝光和二次转化作用。

### 4. Android 端特征总结

Android 购物车不是完全单体系结构，代码层已经做了婚庆和商城双购物车容器，只是当前展示上收敛成一个顶部标题页。

这意味着：

1. UI 上看起来是单购物车页
2. 代码上仍保留业务扩展能力

## 六、我的

### 1. 页面定位

“我的”是 Android 端业务聚合度最高的页面，兼具：

1. 个人中心
2. 订单中心
3. 接单中心
4. 店铺管理后台入口
5. 平台工具集合

承载页面：`MineFragment`

### 2. UI 结构概览

#### 2.1 顶部个人信息区

1. 顶部背景图
2. 设置按钮
3. 头像卡片
4. 用户名
5. 所属团队

#### 2.2 用户数据区

1. 粉丝
2. 关注
3. 余额
4. 现金折扣券

#### 2.3 订单与接单区

1. 婚庆订单
2. 婚庆接单
3. 商城订单
4. 商城接单

每个区域下都不是单按钮，而是一个宫格状态入口，例如：

1. 全部订单
2. 待付款
3. 待接单 / 待发货
4. 待服务 / 待收货
5. 待评价

#### 2.4 工具与管理区

1. 其他工具
2. 店铺管理
3. 常用工具

典型入口包括：

1. 实名认证
2. 我的需求
3. 我的社团
4. 我的邀请
5. 婚礼新闻
6. 活动投票
7. 店铺信息
8. 我的认证
9. 发布档期
10. 发布报价
11. 我的商品
12. 上传图片
13. 上传视频
14. 上传案例
15. 服务城市
16. 推荐团队
17. 店铺主页
18. 发布需求
19. 黄道吉日
20. 电子请柬
21. 日程安排
22. 婚礼宝典
23. 婚礼流程
24. 记账助手
25. 婚姻登记处

#### 2.5 底部菜单区

1. 充值
2. 商家 VIP
3. 用户 VIP
4. 邀请婚嫁商家
5. 申请成为商家
6. 关于我们

### 3. 功能逻辑

1. 在 `MainActivity` 点击“我的”时，未登录先跳登录。
2. 登录后进入页面，会请求 `ApiManager.getUserInfo` 拉取用户中心数据。
3. 页面根据 `user_type` 做差异化展示：
   - `1` 商城商家
   - `2` 婚庆商家
   - `3` 普通用户
4. 不同身份下，订单区、接单区、店铺管理区、VIP 区会动态显示或隐藏。
5. 页面在 `onResume` 和登录成功事件后会刷新用户数据。
6. 粉丝、关注、余额、折扣券都可点击进入下一级页面。
7. 各类订单和接单入口统一跳 `NewHunQinOrderActivity`，通过不同参数区分场景。
8. 店铺管理入口根据商家类型跳不同店铺页。
9. “我的社团”会根据是否已有社团决定跳“创建/我的社团”还是“团队中心”。
10. 未登录时页面会回退为默认头像和“未登录”展示。

### 4. Android 端特征总结

Android 我的页明显比小程序更偏“运营后台式聚合页”，不仅是个人信息页，而是商家和普通用户的多角色入口面板。

核心特点：

1. 模块密度高
2. 身份分流明显
3. 入口数量多
4. 对登录态依赖强

## 七、跨页面共性

### 1. UI 共性

1. 四个主 Tab 都是原生 Android 容器页，不是统一单模板复制。
2. 多数页面顶部自行处理状态栏占位和自定义页头。
3. 首页、发现、购物车都存在“预留二级 Tab，但当前部分隐藏或收敛显示”的情况。

### 2. 功能共性

1. 首页偏内容分发和业务导流。
2. 发现偏社区内容浏览与互动。
3. 购物车偏交易转化。
4. 我的偏账户和商家后台能力聚合。

### 3. 登录态共性

1. 首页和发现允许先浏览后部分操作登录。
2. 购物车和我的在主 Tab 入口层面就有登录拦截。
3. 发布、关注、结算、店铺管理、账户能力等关键动作都依赖登录。

## 八、结论

如果只看 Android 端现状，这四个主 Tab 的职责划分可以理解为：

1. 首页：平台主入口，承担婚庆、社团、案例三类内容分发
2. 发现：圈层内容流与互动页
3. 购物车：交易前管理与结算页
4. 我的：个人中心 + 商家后台 + 工具入口集合页

与小程序版相比，Android 端更明显的特点是：

1. 首页承担更多二级频道职责
2. 发现和购物车都保留扩展结构，但当前 UI 更收敛
3. 我的页的商家化、后台化程度更高

---

生成依据：

- `MainActivity`
- `MainIndexFragment`
- `NewIndexFragment`
- `ClubFragment`
- `FindFragment`
- `fragment.discover.DiscoverFragment`
- `NewCartFragment`
- `WeddingCartFragment`
- `MineFragment`
- `activity_main.xml`
- `activity_main2.xml`
- `base_bottom_menu_layout.xml`
- `index_bar.xml`
