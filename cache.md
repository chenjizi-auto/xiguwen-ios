# 接口缓存策略设计方案

## 1. 目标

本方案用于解决以下问题：

- 字典类接口重复请求，影响首屏速度
- 地区数据、职业分类等基础数据变化慢，适合本地持久化
- 首页列表、发现列表等高频页面需要提升弱网可用性
- 部分页面对缓存命中是同步回调，容易因为 `execute`/`subscribe` 顺序错误导致数据丢失
- 后续还会有更多接口接入缓存，需要一套统一策略，而不是各页面各自处理

本方案当前已经在项目中落地的方向包括：

- 地区接口：`appapi/system/huoqudiqu`
- 职业分类接口：`appapi/Home/Classificationlist`
- 首页列表
- 发现列表
- 发现详情
- 店铺/人物详情部分示例

---

## 2. 总体策略

按接口类型分层处理，不做“一刀切”。

### 2.1 基础字典类接口

特点：

- 数据变化慢
- 数据量相对固定
- 适合长 TTL
- 页面依赖强

适用接口：

- 地区
- 职业分类
- 其他枚举、分类、配置字典

策略：

- 启动时预加载
- 网络成功后写入 sqlite
- 页面读取时优先本地缓存，后台再刷新
- TTL 建议 7 天
- 允许使用过期缓存兜底

### 2.2 列表类接口

特点：

- 数据更新频率中等
- 参数组合多
- 用户对“先看到旧数据，再刷新新数据”容忍度较高

适用接口：

- 首页列表
- 发现列表
- 分类列表
- 推荐流、分页流

策略：

- 按 `接口路径 + 参数 + 用户` 生成唯一 cacheKey
- 优先返回缓存，再发网络刷新
- 首屏列表建议缓存第一页
- 分页数据不建议长时间缓存全部页
- TTL 建议 5 分钟到 30 分钟

### 2.3 详情类接口

特点：

- 数据通常和某个 id 强绑定
- 刷新频率低于列表
- 对弱网体验帮助明显

适用接口：

- 发现详情
- 店铺详情
- 人物详情

策略：

- 按详情 id 缓存
- 先回缓存，再刷新网络
- TTL 建议 30 分钟到 24 小时，按业务决定

### 2.4 敏感/强实时接口

特点：

- 数据实时性高
- 对正确性要求高

不建议直接缓存接口结果：

- 下单
- 支付
- 余额
- 消息未读数
- 审核状态强实时接口

策略：

- 不缓存或仅做短时内存缓存
- 必须以网络结果为准

---

## 3. 本地存储设计

当前方案使用 sqlite，底层通过 FMDB 管理。

### 3.1 表设计

#### `api_cache`

用于存放通用接口缓存。

主要字段：

- `cache_key`：唯一缓存键
- `api_path`：接口路径
- `params_md5`：参数摘要
- `user_id`：用户维度隔离
- `data_json`：原始 JSON
- `expired_at`：过期时间
- `updated_at`：更新时间
- `created_at`：创建时间

用途：

- 列表缓存
- 详情缓存
- 字典接口原始 JSON 兜底

#### `region`

用于地区数据结构化存储。

主要字段：

- `region_id`
- `parent_id`
- `region_name`
- `region_level`
- `sort`
- `initial`
- `pinyin`
- `status`
- `is_new`
- `full_name`
- `payload_json`
- `updated_at`

用途：

- 省市区级联查询
- 根据 `provinceid/cityid/countyid` 反查显示名称
- 页面地址展示不再直接拼 id

#### `cache_meta`

用于后续扩展缓存元信息。

主要字段：

- `cache_key`
- `etag`
- `server_version`
- `last_sync_at`

当前先保留扩展位，后续可以做增量同步或版本控制。

---

## 4. cacheKey 设计

统一规则：

`cache_key = api_path + user_id + params_md5`

目的：

- 同接口不同参数不冲突
- 同接口不同用户不串缓存
- 列表/详情都可统一接入

特殊接口允许固定 key：

- 地区：`appapi/system/huoqudiqu_global`
- 职业分类：`appapi/Home/Classificationlist_global`

适用场景：

- 全局基础数据
- 不区分用户

---

## 5. 读写流程

### 5.1 通用接口缓存流程

1. 根据接口路径、参数、用户生成 `cacheKey`
2. 先读 `api_cache`
3. 若命中缓存，立即回调给页面
4. 再发网络请求
5. 网络成功后覆盖本地缓存
6. 再次回调最新数据
7. 网络失败时，如果已有缓存则静默兜底；无缓存则抛错

### 5.2 地区接口流程

1. App 启动时预加载 `appapi/system/huoqudiqu`
2. 若 `api_cache` 中已有原始 JSON，先恢复到 `region` 表
3. 发网络请求刷新
4. 网络成功后：
   - 保存原始 JSON 到 `api_cache`
   - 解析并重建 `region` 表
5. 页面使用时优先读本地缓存，不阻塞 UI

### 5.3 页面接入顺序要求

必须遵守：

1. 先 `subscribe`
2. 再 `execute`

原因：

- 缓存命中时可能同步 `sendNext`
- 如果先 `execute` 再订阅，页面会错过缓存数据

这类问题已经在部分页面出现过，后续所有缓存接口都必须按这个顺序写。

---

## 6. 启动预加载策略

当前已将地区接口放到 App 启动阶段预加载。

建议启动预加载仅处理“高复用、低频更新、全局依赖”的接口：

- 地区
- 职业分类
- 城市站点
- 全局配置

不建议启动阶段预加载：

- 首页大列表
- 发现流分页
- 用户详情
- 重流量接口

原因：

- 冷启动阶段不应堆太多网络
- 否则会拉长启动链路

推荐原则：

- 启动阶段只拉基础字典
- 页面阶段拉页面专属业务数据

---

## 7. TTL 建议

可按接口类型统一规划：

- 地区：7 天
- 职业分类：7 天
- 全局配置：1 天
- 首页列表第一页：10 分钟
- 发现列表第一页：10 分钟
- 详情页：30 分钟到 1 天

说明：

- TTL 到期不代表必须删数据
- 可以继续保留过期缓存做兜底
- 网络成功后覆盖即可

---

## 8. 数据一致性策略

### 8.1 以网络为最终真值

规则：

- 缓存只负责提速和兜底
- 最终结果仍以服务端返回为准

### 8.2 更新接口后主动失效相关缓存

例如：

- 编辑店铺信息后，清理店铺详情缓存
- 发布/删除发现内容后，清理发现列表和详情缓存
- 更新首页配置后，清理首页列表缓存

建议后续增加：

- 按接口前缀清理
- 按业务模块批量清理
- 按用户清理

### 8.3 地区名称展示策略

页面展示地址时，不直接显示：

- `provinceid`
- `cityid`
- `countyid`

而是通过 `region` 表反查名称后再展示。

原因：

- 后端常返回的是 id，不是名称
- 直接拼接会导致“地址显示为空”或“显示成数字”

---

## 9. 当前已落地的代码结构

### 9.1 缓存核心层

目录：

- `xiguwen/Help/Cache/CwDBManager.*`
- `xiguwen/Help/Cache/CwApiCacheStore.*`
- `xiguwen/Help/Cache/CwCacheableRequestHelper.*`

职责：

- `CwDBManager`
  - 管理 sqlite 路径与 FMDB 队列

- `CwApiCacheStore`
  - 建表
  - 读写 `api_cache`
  - 管理 `region`
  - 地区 id 反查名称

- `CwCacheableRequestHelper`
  - 封装“先缓存后网络”的请求流程

### 9.2 页面接入层

已示例接入的页面包括：

- 首页列表
- 发现列表
- 发现详情
- 店铺信息地区数据
- 职业分类

页面接入原则：

- 业务层只关心“拿缓存 + 刷网络”
- 不直接拼 sqlite 读写逻辑
- 缓存细节收口在 `Help/Cache`

---

## 10. 后续推荐扩展

### 10.1 增加缓存清理能力

建议补充接口：

- 按 `cacheKey` 删除
- 按 `apiPath` 删除
- 删除某个用户所有缓存
- 清理过期缓存

### 10.2 增加缓存版本控制

可利用 `cache_meta`：

- 服务端返回版本号
- 本地记录版本号
- 版本变化时强制刷新

### 10.3 增加内存缓存

对于首页、发现首页这种频繁回切页面：

- 可在 sqlite 之上再加一层 NSCache
- 提升二次进入速度

建议分层：

- 内存缓存：秒级命中
- sqlite 缓存：进程重启后仍可用
- 网络：最终真值

### 10.4 增加埋点监控

建议统计：

- 缓存命中率
- 各接口平均耗时
- 启动预热成功率
- fallback 使用次数
- 过期缓存命中次数

这样后续才能判断哪些接口值得缓存、哪些接口缓存收益低。

---

## 11. 落地规范

后续新接口若要接缓存，统一按下面规则执行：

1. 先判断接口类型
   - 字典 / 列表 / 详情 / 强实时
2. 明确缓存 key 规则
3. 明确 TTL
4. 明确是否允许过期缓存兜底
5. 页面必须先订阅再执行
6. 更新类接口必须考虑相关缓存失效
7. 展示型字段优先存原始 JSON，避免重构后字段丢失

---

## 12. 当前方案结论

当前项目最合适的缓存方案不是“所有接口全缓存”，而是：

- 字典类接口：sqlite 持久缓存 + 启动预热
- 列表类接口：按参数分页缓存第一页，先缓存后网络
- 详情类接口：按 id 缓存，先缓存后网络
- 强实时接口：不缓存或仅做极短缓存

这套方案的优点是：

- 接入成本低
- 对旧项目侵入小
- 能明显改善首屏和弱网体验
- 后续可以平滑扩展

这也是当前项目最适合继续推进的方向。
